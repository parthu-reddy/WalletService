apiVersion: v1
kind: Service
metadata:
  name: wallet-service
  namespace: food-delivery
spec:
  selector:
    app: wallet-service
  ports:
    - protocol: TCP
      port: 8120
      targetPort: 8120
