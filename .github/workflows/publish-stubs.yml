name: Publish contract stubs

# Manual only, matching the platform-wide decision that no pipeline runs on its own.
on:
  workflow_dispatch:

jobs:
  publish-stubs:
    name: Publish wallet-service stubs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          path: WalletService

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: temurin
          cache: maven

      # NOT -Pci-tests. That profile sets spring.cloud.contract.verifier.skip=true, and with the
      # verifier skipped no stubs jar is produced at all -- the build succeeds and emits nothing to
      # publish.
      #
      # Tests are NOT skipped here. They used to be, on the grounds that they 'run in this service's
      # own build' -- but that build is build-and-push.yml, which uses -Pci-tests and therefore never
      # generates them. The result was that no workflow anywhere ran a producer contract test, and
      # every stubs jar in the registry was an unverified recording.
      - name: Build the stubs jar
        env:
          PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
        run: mvn -B clean package -s WalletService/.github/maven-settings.xml -f WalletService/pom.xml

      # The stubs jar is a recording, and publishing one that was never verified is how a consumer
      # ends up testing against a contract its producer does not honour. Running the tests above is
      # only half of it: if generation silently produces nothing, `package` stays green and there is
      # nothing to run. Assert the tests were both generated and reported.
      - name: Confirm the producer contract tests actually ran
        run: |
          GEN_DIR="WalletService/target/generated-test-sources/contracts"
          REPORTS="WalletService/target/surefire-reports"
          GEN=$(find "$GEN_DIR" -name '*Test.java' 2>/dev/null | wc -l | tr -d ' ')
          if [ "$GEN" -eq 0 ]; then
            echo "::error::No producer contract tests were generated. The stubs jar would be published unverified."
            exit 1
          fi
          MISSING=0
          for f in $(find "$GEN_DIR" -name '*Test.java' 2>/dev/null); do
            b=$(basename "$f" .java)
            if ! ls "$REPORTS"/TEST-*."$b".xml >/dev/null 2>&1; then
              echo "::error::Generated contract test $b was compiled but never ran."
              MISSING=1
            fi
          done
          [ "$MISSING" -eq 0 ] || exit 1
          echo "OK: $GEN generated producer contract test class(es) generated and executed"

      # deploy:deploy-file, not deploy. A bare `mvn deploy` would also publish the main artifact --
      # a Spring Boot fat jar, 118 MB for WalletService -- into a registry whose whole purpose is to
      # hold 4 KB stub jars.
      #
      # Everything goes to ONE registry because StubRunnerProperties.repositoryRoot is a single
      # Resource, not a list, so a consumer can point at exactly one. Cross-repository writes need
      # the classic PACKAGES_TOKEN; the per-run GITHUB_TOKEN is scoped to its own repository.
      - name: Publish the stubs jar
        env:
          PACKAGES_TOKEN: ${{ secrets.PACKAGES_TOKEN }}
        run: |
          STUBS=$(ls WalletService/target/*-stubs.jar)
          echo "publishing $STUBS ($(du -h "$STUBS" | cut -f1))"
          mvn -B deploy:deploy-file \
            -DrepositoryId=github-contracts \
            -Durl=https://maven.pkg.github.com/parthu-reddy/FoodDeliveryContracts \
            -Dfile="$STUBS" \
            -DgroupId=com.fooddelivery \
            -DartifactId=wallet-service \
            -Dversion=1.0-SNAPSHOT \
            -Dclassifier=stubs \
            -Dpackaging=jar \
            -s WalletService/.github/maven-settings.xml
